# VS Code(Claude Code) 프롬프트 — FastAPI 백엔드 + 사내 웹배포 (기밀 보호 강화판)

먼저 준비: 이전에 받은 JSON 4개(`company_sizes.json`, `shareholder_holdings.json`, `intercompany_holdings.json`, `params.json`)를 프로젝트의 **`backend/data/`** 에만 넣으세요. (프론트엔드 폴더에는 절대 넣지 마세요.)
그다음 아래 블록 전체를 복사해 Claude Code 채팅창에 붙여넣으세요.

---

일감몰아주기 증여세 대상 여부를 각 법인이 스스로 확인하는 **사내 웹앱**을 만들어줘. **FastAPI 백엔드 + 정적 프론트엔드** 구조로, 지분율 원본이 클라이언트로 절대 나가지 않게 설계해줘. 아래 사양을 정확히 지켜라.

## 0. 보안·프라이버시 (최우선, 위반 금지)
- 지분율 데이터(`backend/data/`의 4개 JSON)는 **백엔드 프로세스 안에서만** 로드한다. 프론트엔드 번들·정적 폴더·git에 커밋되는 공개 경로에 절대 두지 않는다.
- **어떤 API 응답에도 원본 지분율·지배주주 개인 지분·회사 간 소유구조(지분도)를 넣지 않는다.** API는 계산 결과(집계값)만 반환한다.
- 반환 허용 필드: `과세대상여부(bool)`, `특관매출`, `총매출`, `특관매출비율`, `규모`, `정상거래비율`, `총증여의제이익`, `총증여세`, `판정근거문구`. 그 외(지분율 수치, 지배주주 명단, 개인별 증여세, 타 법인 데이터)는 반환 금지.
- **법인별 접근통제**: 각 법인은 자기 법인 결과만 조회. 로그인(법인 계정) 또는 법인별 접근코드 방식. 관리자 계정만 전체 열람(관리자 화면은 `/admin`, 별도 인증).
- 로그·에러메시지·디버그 출력에 지분율을 남기지 않는다. 서버 로그는 입력 매출/결과 요약까지만.
- CORS는 사내 도메인만 허용. 프로덕션은 HTTPS 강제.

## 1. 데이터 (`backend/data/`)
- `company_sizes.json` : `{법인명: "일반"|"중견"|"중소"}` (50개)
- `shareholder_holdings.json` : `{법인명: {"A":..,"B":..,"C":..,"D":..,"C1":..,"C11":..,"C12":..,"sum":..}}` — 지배주주 7인의 그 법인에 대한 직·간접 합계지분율(0~1). **기밀.**
- `intercompany_holdings.json` : `{소유법인: {피소유법인: 직·간접지분율}}` — 제10항 판정용. **기밀.**
- `params.json` : 지배주주 코드/이름, 규모별 정상거래비율·공제거래비율·공제보유비율, 누진세율표, 면세점(50만원), 기준시점.

## 2. 백엔드 (FastAPI, `backend/`)
파일: `main.py`(앱·라우팅·인증), `calc.py`(순수 계산), `models.py`(pydantic 스키마), `auth.py`, `data/`(JSON), `tests/test_calc.py`, `requirements.txt`.

엔드포인트:
- `GET /api/companies` → 로그인한 법인이 선택 가능한 **거래처 목록(법인명 문자열 배열) + "기타법인"** 만 반환. (규모·지분 등 부가정보 없이 이름만.) "기타법인"은 개별 법인으로 잡히지 않는 나머지 거래처용 catch-all 이며, 판정 대상 법인으로는 선택할 수 없다.
- `GET /api/my-company` → 인증된 사용자의 법인명·규모만.
- `POST /api/evaluate` → 입력을 받아 계산 결과(위 0의 허용 필드)만 반환. 요청 법인 ≠ 인증 법인이면 403.
- `POST /api/auth/login`, 관리자용 `GET /api/admin/summary`(전체 법인 과세대상 여부·총증여세 목록; 인증 필요).

`POST /api/evaluate` 요청 스키마:
```
{ "company": "시지바이오",
  "operating_income": 40523642066,   // ① 영업손익 (세무조정 '전'. 조정액은 tax_adjustments 로)
  "corporate_tax": 0,                // ② 법인세상당액 (모르면 0)
  "total_sales": 175957772230,       // ④ 총매출액(전체)
  "related_sales": { "대웅제약": 6954928740, "대웅": 117332702, "기타법인": 0 },
  "tax_adjustments": { "감가상각비": 3000000000, "퇴직급여충당금": -1000000000 }
                                     // 가산은 양수, 차감은 음수. 세후영업이익 = ① ± 조정합계 - ②
}
```
※ `indirect_invest` 는 받지 않는다. 간접출자법인 여부는 서버가 판정한다(아래 과세제외 로직 참조).

### 과세제외 로직 (상증령 §34의3)
거래처 1건 × 지배주주 1인 단위로 판정한다.
1. **⑩ 기본 과세제외 우선** — 수혜법인이 해당 거래처에 출자한 경우(`INTER[수혜법인][거래처] > 0`) 매출액 전액 제외하고 종료. ⑭ 는 보지 않는다.
2. **⑭ 추가 과세제외** — ⑩ 미해당 거래처에만 적용. 후보:
   - **⑭1호(§18 간접출자법인)**: `data/section18_indirect_investors.json` 에 등재된 관계. 매출액 **100% 전액 제외**.
   - **⑭ 지분율 상당액**: 지배주주의 해당 거래처 지분율만큼 제외.
3. **사유가 겹치면 합산하지 않고 과세제외금액이 가장 큰 하나만** 적용한다.

단순 간접지분 관계(지분 경로가 연결돼 있을 뿐인 경우)는 §18 간접출자법인이 아니며 전액 제외 대상이 아니다. §18 은 세무 검토로 확정된 관계만 데이터 파일에 등재한다.

응답의 `exclusion_details` 에 거래처별 **과세제외 사유·적용 조문·적용률·과세제외매출액**이 실린다. 일반 응답(`/api/evaluate`)에도 그대로 나간다.

- ⑩·§18 처럼 지배주주와 무관하게 같은 율인 건: `rate`/`excluded_sales` 에 단일값.
- ⑭ 지분율 상당액처럼 주주마다 다른 건: `rate`/`excluded_sales` 는 `null`, 대신 `rate_min`/`rate_max`/`excluded_sales_min`/`excluded_sales_max` 범위로 제공.
- `by_shareholder`(지배주주 개인별 적용률=지분율)는 관리자 응답(`/api/admin/evaluate-review`)에만 붙는다.

응답 스키마:
```
{ "taxable": true, "size": "중견",
  "related_sales_total": 53523457366, "total_sales": 175957772230,
  "related_sales_ratio": 0.3046, "normal_ratio": 0.40,
  "deemed_gift_total": 0, "gift_tax_total": 0,
  "reason": "…판정 근거…" }
```

## 3. 계산 로직 (`calc.py` — 엑셀 검증본과 100% 동일)
지배주주 7인: A,B,C,D,C1,C11,C12. 선택 법인=s, 거래처=g, 지배주주=k.
거래처별:
- `제10항_g` = ( `intercompany_holdings[s].get(g,0) > 0` 이면 `매출_g`, 아니면 0 )
- `F_g` = `매출_g - 제10항_g`
- `⑭_gk` = `max( (indirect_invest[g] 가 True 면 F_g, 아니면 0), F_g * shareholder_holdings.get(g,{}).get(k,0) )`
법인 합계 / 지배주주별:
- `특관매출` = Σ 매출_g ;  `제10항합` = Σ 제10항_g ;  `⑭합_k` = Σ_g ⑭_gk
- `거래비율_k` = ( `특관매출 - (제10항합+⑭합_k)` ) / ( `총매출 - (제10항합+⑭합_k)` )  (분모 0이면 0)
- `보유비율_k` = `shareholder_holdings[s].get(k,0)`
- `세후영업이익_k` = `(operating_income - corporate_tax) * (1 - (제10항합+⑭합_k)/총매출)`  (총매출 0이면 0)
- `증여의제이익_k` = `max(0,세후영업이익_k) * max(0, 거래비율_k - 공제거래비율[규모]) * max(0, 보유비율_k - 공제보유비율[규모])`
- `증여세_k` = 증여의제이익_k < 500000 이면 0, 아니면 `내림10원( 과표*세율 - 누진공제 )`; 누진세율표=`[[0,.10,0],[1e8,.20,1e7],[5e8,.30,6e7],[1e9,.40,1.6e8],[3e9,.50,4.6e8]]`(과표 이하 최대 구간).
법인 단위:
- `taxable` = (Σ_k 증여세_k > 0)
- `deemed_gift_total` = Σ 증여의제이익_k ;  `gift_tax_total` = Σ 증여세_k
- `related_sales_ratio` = 특관매출/총매출 (표시용 참고치)
- `calc.py`는 **지배주주별 내역·지분율을 반환하지 않고** 위 집계 dict만 반환한다.

## 4. 프론트엔드 (`frontend/`, 지분 데이터 없음)
- 순수 정적(HTML+최소 JS) 또는 Vite. 지분율 관련 데이터를 번들에 넣지 않는다. 거래처 목록은 `GET /api/companies`로 런타임 조회.
- 화면: 로그인 → 자기 법인·규모 표시 → ①②④ 입력 + 거래처별 매출 입력표(+간접출자 체크) → "판정" 버튼 → 결과 카드.
- 결과: 큰 배지(**과세대상 / 해당없음**), 카드(특관매출, 총매출, 특관매출비율%, 규모, 정상거래비율%), 카드(총 증여의제이익, 총 증여세 추정), 판정근거 문구.
- 주의문구: "지분율·규모는 2026.06.30 기준 내부 데이터로 자동 반영. 개략 추정이며 최종 신고 전 세무대리인 검토 필요. 법인세상당액 미입력 시 증여세 과대 산출." **지분율·지배주주·타 법인 정보 표시 금지.**

## 5. 배포 (사내 공유)
- `docker-compose.yml`: `backend`(FastAPI+uvicorn), `frontend`(nginx 정적 서빙 + `/api` 역프록시). `backend/data`는 이미지 내부 또는 마운트 볼륨으로만, 외부 공개 경로 아님.
- `.env`(git 제외): 관리자 비밀번호/JWT 시크릿/허용 CORS 도메인. `.env.example` 제공.
- `README.md`: 로컬 실행(`uvicorn`/`vite`)과 사내 배포(`docker compose up -d`, 사내망/HTTPS/역프록시) 설명. 인증·법인 계정 발급 방법.
- `.gitignore`에 `backend/data/*.json`(민감 데이터는 배포 파이프라인에서 주입) 또는 최소 `.env` 포함. 데이터 취급 방침을 README에 명시.

## 6. 검증 (필수, 먼저 통과)
`backend/tests/test_calc.py`:
- 이지메디컴: total_sales=1e10, operating_income=1e10, corporate_tax=0, related_sales={"대웅제약":9e9} → gift_tax_total == 1,559,826,490
- 대웅펫: total_sales=8e9, operating_income=5e9, corporate_tax=0, related_sales={"대웅제약":5e9} → gift_tax_total == 22,634,130
먼저 `calc.py`로 두 테스트를 통과시킨 뒤 API·인증·프론트·배포 순으로 완성해줘. 그리고 "지분율·개인별 데이터가 어떤 API 응답에도 포함되지 않는다"는 것을 확인하는 테스트도 추가해줘.
